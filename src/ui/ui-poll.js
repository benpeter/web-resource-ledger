// Stub: polling module.
// Placeholder until the full polling implementation is built.

export const POLL_JS = `
// ---------------------------------------------------------------------------
// Polling (stub)
// ---------------------------------------------------------------------------

// pollCapture: polls a capture until it reaches a terminal state.
// Will be wired into the detail view when implemented.
function pollCapture(id, onUpdate) {
  // Stub: no-op until polling is implemented.
  void id;
  void onUpdate;
}

function stopPolling() {
  // Stub: no-op until polling is implemented.
}
`;
