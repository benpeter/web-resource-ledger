---
phase: 00-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/ui-billing.js
  - src/scheduler.js
  - src/ui/ui-submit.js
  - test/scheduled-handler.test.js
  - docs/evolution/0108-pre-flight-cleanup/prompt.md
  - docs/evolution/0108-pre-flight-cleanup/decisions.md
  - docs/evolution/0108-pre-flight-cleanup/outcome.md
  - docs/evolution/README.md
  - docs/backlog.md
autonomous: false
requirements:
  - PRE-01
  - PRE-02
  - PRE-03
  - QG-04
  - QG-05
  - QG-06
  - QG-07
must_haves:
  truths:
    - "Billing dashboard renders the grace-period/blocked alert banner for tenants in those billing states (no more stray 'Status: Pending' for grace_period or blocked)."
    - "When `advanceSchedule` fails on a threat-blocked schedule, an error log line `schedule.advance_failed` (severity 5, subsystem `schedule`) appears in Coralogix."
    - "There is no `.catch(() => {})` (silent catch) anywhere in `src/scheduler.js`."
    - "`src/ui/*.js` declares `formatDate` exactly once (in `ui-settings.js`); `submit_formatDate` is the prefixed twin in `ui-submit.js`."
    - "`docs/evolution/0108-pre-flight-cleanup/{prompt,decisions,outcome}.md` exist; `outcome.md` includes a `## Files changed` table and a `## Backlog changes` section (even if it explicitly says no changes)."
  artifacts:
    - path: "src/ui/ui-billing.js"
      provides: "Renamed function `billing_buildStatusBanner` (was `buildStatusBanner`) and updated single callsite."
      contains: "function billing_buildStatusBanner"
    - path: "src/scheduler.js"
      provides: "Structured error logging on `advanceSchedule` failure for threat-blocked schedules."
      contains: "schedule.advance_failed"
    - path: "src/ui/ui-submit.js"
      provides: "Renamed function `submit_formatDate` (was `formatDate`) and updated single callsite."
      contains: "function submit_formatDate"
    - path: "docs/evolution/0108-pre-flight-cleanup/prompt.md"
      provides: "QG-04 evolution log -- prompt"
      min_lines: 10
    - path: "docs/evolution/0108-pre-flight-cleanup/decisions.md"
      provides: "QG-04 evolution log -- decisions (PRE-03 disambiguation, log severity choice, deploy gate)"
      min_lines: 15
    - path: "docs/evolution/0108-pre-flight-cleanup/outcome.md"
      provides: "QG-04 evolution log -- outcome with Files changed table + Backlog changes section (QG-05)"
      min_lines: 15
  key_links:
    - from: "src/ui/ui-billing.js:169"
      to: "src/ui/ui-billing.js:198"
      via: "function call -> declaration after rename"
      pattern: "billing_buildStatusBanner\\(usageData\\)"
    - from: "src/scheduler.js (catch block at advanceSchedule call)"
      to: "src/log.js"
      via: "structured log emission"
      pattern: "log\\(env, *5, *'schedule', *\\{[\\s\\S]*?event: *'schedule\\.advance_failed'"
    - from: "src/ui/ui-submit.js:438"
      to: "src/ui/ui-submit.js:25"
      via: "function call -> declaration after rename"
      pattern: "submit_formatDate\\(data\\.resetsAt\\)"
---

<objective>
Plan A — Pre-flight cleanup. Land three surgical fixes (PRE-01, PRE-02, PRE-03) in
a single PR with three atomic commits. Deploy to staging, smoke-test, then promote
to production. This must merge and deploy before Plan B starts measuring (D-14):
PRE-02 makes previously-silent scheduler errors visible in Coralogix, so baselines
must be measured against the post-fix system.

Purpose:
- Eliminate the active production bug where the billing grace-period/blocked banner
  is shadowed by `ui-detail.js`'s `buildStatusBanner` (PRE-01).
- Stop the silent swallowing of D1 write failures on threat-blocked schedule advance,
  which today causes blocked schedules to potentially re-fire every minute (PRE-02).
- Resolve the harmless-today duplicate `formatDate` declaration so it stops
  violating the UI prefix rule (PRE-03).
- Establish QG-04..07 process compliance (evolution log, backlog review, log
  discipline, UI prefix) as the canonical demonstration for the milestone.

Output:
- 3 file edits in `src/`, 1 evolution-log directory under `docs/evolution/0108-pre-flight-cleanup/`,
  optional new test case in `test/scheduled-handler.test.js`, optional `docs/backlog.md`
  edit (or explicit "no changes" record in outcome.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-PATTERNS.md
@.planning/phases/00-foundation/00-VALIDATION.md
@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- Key existing functions and conventions the executor will reuse. Extracted from codebase. -->

From src/log.js (already wired to Coralogix; envelope returns undefined when bindings absent):
```js
// log(env, severity, subsystem, data) -> Promise|undefined
// Severity: 3=info/lifecycle, 4=warning, 5=error.
// Used in scheduler.js with the envelope: ctx.waitUntil(log(...) ?? Promise.resolve())
```

From src/scheduler.js:139-145 (sibling pattern for `schedule.blocked_threat` -- the
event emitted just AFTER the silent catch site at line 138):
```js
ctx.waitUntil(log(env, 4, 'schedule', {
  event: 'schedule.blocked_threat',
  scheduleId: schedule.id,
  tenantId,
  url: schedule.url,
  threatTypes: threat.threatTypes,
}) ?? Promise.resolve());
```

From src/scheduler.js:161-172 (sibling pattern for `schedule.execute_fail` -- D1
write failure with severity 5; copy this shape exactly for the new emit):
```js
} catch (err) {
  ctx.waitUntil(log(env, 5, 'schedule', {
    event: 'schedule.execute_fail',
    scheduleId: schedule.id,
    tenantId,
    url: schedule.url,
    captureId: captureId ?? null,
    errorMessage: String(err?.message ?? '').slice(0, 128),
  }) ?? Promise.resolve());
  skippedCount++;
  continue;
}
```

From src/ui/ui-detail.js:34 (do NOT modify -- the survivor of the collision; only
billing-side renames):
```js
function buildStatusBanner(status) {
  var banner = document.createElement('div');
  banner.className = 'detail-status-banner';
  // ...
}
```

From src/ui/ui-billing.js:169 + :198 (the rename targets, exact lines per RESEARCH.md):
```js
// :169 (call site)
var banner = buildStatusBanner(usageData);
// :198 (declaration)
function buildStatusBanner(usageData) { ... }
```

From src/ui/ui-submit.js:25 + :438 (rename targets per RESEARCH.md PRE-03):
```js
// :25 (declaration; byte-identical body to ui-settings.js:22)
function formatDate(isoStr) { ... }
// :438 (call site)
var resetDate = data.resetsAt ? formatDate(data.resetsAt) : '';
```

UI prefix convention (per CLAUDE.md §"Dashboard UI Architecture"):
`<view>_<function>` where `<view>` is the file's view name without the `ui-` prefix.
So `ui-billing.js` adds prefix `billing_`; `ui-submit.js` adds `submit_`.

Evolution log shape (per CLAUDE.md §"Evolution Log" + analog
`docs/evolution/0107-stripe-authoritative-billing/`):
- `prompt.md`: H1 phase title, `## Task`, `## Approach`, `## Prior Work`.
- `decisions.md`: H1 "Decisions -- Phase NNNN", one H2 per decision, **Decision** in
  bold followed by rationale.
- `outcome.md`: H1 "Outcome -- Phase NNNN", `## What was built`, `## Files changed`
  table, `## Backlog changes` section (the QG-05 hook -- explicit even if empty).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task A-01: PRE-01 -- Rename buildStatusBanner in ui-billing.js</name>
  <files>src/ui/ui-billing.js</files>
  <read_first>
    - src/ui/ui-billing.js (lines 160-210 -- both the call site at 169 and the declaration at 198)
    - src/ui/ui-detail.js (lines 28-58 -- confirm the survivor function is left alone)
    - src/ui/ui-shell.js (confirm concatenation order: SETTINGS_JS -> BILLING_JS -> SUBMIT_VIEW_JS -> DETAIL_VIEW_JS)
    - .planning/phases/00-foundation/00-RESEARCH.md §PRE-01 (exact diffs)
    - .planning/phases/00-foundation/00-PATTERNS.md §"src/ui/ui-billing.js (modify, PRE-01)"
  </read_first>
  <action>
    Rename ONLY the billing-side `buildStatusBanner` to `billing_buildStatusBanner`
    per D-PRE-01 in CONTEXT.md. Do NOT touch `src/ui/ui-detail.js`.

    Exactly two line edits in `src/ui/ui-billing.js`:

    Line 169 (call site -- inside the billing view render flow):
    ```diff
    - var banner = buildStatusBanner(usageData);
    + var banner = billing_buildStatusBanner(usageData);
    ```

    Line 198 (function declaration):
    ```diff
    - function buildStatusBanner(usageData) {
    + function billing_buildStatusBanner(usageData) {
    ```

    Total: 2 lines changed in `src/ui/ui-billing.js`. No other files touched.
    No test changes (UI string code is not unit-tested).

    Commit message (atomic per WRL convention -- per CONTEXT.md "Atomic-commit-per-fix"):
    `fix(ui-billing): resolve buildStatusBanner collision (PRE-01)`
    Body should reference: "fixes grace-period/blocked banner shadowed by
    ui-detail.js buildStatusBanner due to flat UI scope (CONCERNS.md §Scope Collision)."
  </action>
  <verify>
    <automated>
    # QG-07 grep gates (must all pass):
    bash -c '
      set -e
      # Only ui-detail.js may declare buildStatusBanner now:
      MATCHES=$(grep -nE "function +buildStatusBanner\\b" src/ui/*.js)
      echo "$MATCHES"
      [ "$(echo "$MATCHES" | grep -c -v "^$")" = "1" ] || { echo "FAIL: more than one buildStatusBanner declaration"; exit 1; }
      echo "$MATCHES" | grep -q "src/ui/ui-detail.js:" || { echo "FAIL: surviving buildStatusBanner is not in ui-detail.js"; exit 1; }
      # Both billing-side references must resolve to the new name:
      grep -nE "\\bbilling_buildStatusBanner\\b" src/ui/ui-billing.js | grep -E ":(169|198):" || { echo "FAIL: missing rename at expected lines"; exit 1; }
      # Confirm the old name is gone from ui-billing.js:
      ! grep -nE "\\bbuildStatusBanner\\b" src/ui/ui-billing.js || { echo "FAIL: old name still present in ui-billing.js"; exit 1; }
      echo "OK: PRE-01 rename complete"
    '
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "function +buildStatusBanner\b" src/ui/*.js` returns exactly one line, in `src/ui/ui-detail.js`.
    - `grep -nE "\bbilling_buildStatusBanner\b" src/ui/ui-billing.js` returns 2 matches (line 169 call + line 198 decl).
    - `grep -nE "\bbuildStatusBanner\b" src/ui/ui-billing.js` returns ZERO matches.
    - `git diff --stat src/ui/ui-billing.js` shows exactly 2 changed lines (1 insertion + 1 deletion at 169, 1 insertion + 1 deletion at 198 -- a +2/-2 stat).
    - No other files modified by this commit (`git diff --name-only HEAD` shows only `src/ui/ui-billing.js`).
  </acceptance_criteria>
  <done>
    Atomic commit `fix(ui-billing): resolve buildStatusBanner collision (PRE-01)`
    is created with exactly two line changes in `src/ui/ui-billing.js`. All grep
    gates pass. Ready for staging deploy + visual confirmation in Task A-04.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task A-02: PRE-02 -- Replace silent catch in scheduler.js with structured log</name>
  <files>src/scheduler.js, test/scheduled-handler.test.js</files>
  <read_first>
    - src/scheduler.js (lines 100-200 -- full threat-block branch + sibling execute_fail at 161-172)
    - src/log.js (confirm log() signature and Coralogix wiring)
    - test/scheduled-handler.test.js (existing describe blocks; check whether a threat-block + advanceSchedule-throw case already exists with `grep -nE 'advance_failed|threat-block|advanceSchedule.*throw' test/scheduled-handler.test.js`)
    - .planning/phases/00-foundation/00-RESEARCH.md §PRE-02 (exact replacement code, lines 192-208)
    - .planning/phases/00-foundation/00-PATTERNS.md §"src/scheduler.js (modify, PRE-02, line 138)"
    - CLAUDE.md §"Engineering Philosophy" -- "Log to Coralogix, never console" + "Fail loudly, degrade intentionally"
  </read_first>
  <behavior>
    Test cases the new test must cover (TDD: write the test first, RED, then GREEN
    by adding the log emission):

    - Test 1 (PRE-02 happy path stays unchanged): `scheduled handler -- threat-block
      with advanceSchedule succeeding` -- existing behavior preserved; `schedule.blocked_threat`
      still emitted; `skippedCount` increments; no `schedule.advance_failed` log fires.
    - Test 2 (PRE-02 RED then GREEN): `scheduled handler -- threat-blocked schedule
      where advanceSchedule throws emits schedule.advance_failed`. Force `advanceSchedule`
      (or its underlying D1 write) to throw, then assert that:
        a. The handler does NOT crash (the catch still catches).
        b. A log call is made with `severity=5, subsystem='schedule', event='schedule.advance_failed'`,
           with fields `{ scheduleId, tenantId, url, blockReason: 'threat', errorClass, errorMessage }`.
        c. `errorMessage` is clamped to 128 characters.
        d. `skippedCount` still increments and the loop continues to the next schedule.

    Verify-then-add: run `grep -nE 'advance_failed|threat-block|advanceSchedule' test/scheduled-handler.test.js`
    first; if the case already exists, refine it. If absent, add a new `describe`
    block "scheduled handler -- threat-blocked + advanceSchedule fails" with one
    `it()` covering Test 2.
  </behavior>
  <action>
    Two edits, one to source and one (optional, see verify-then-add) to tests.

    1) `src/scheduler.js` line 138 -- replace the silent catch with the structured
       log emission. Verbatim from RESEARCH.md (matches sibling `schedule.execute_fail`
       at lines 161-172):

    ```diff
    - await advanceSchedule(env.DB, schedule.id, nextRunAt, null, 'blocked').catch(() => {});
    + await advanceSchedule(env.DB, schedule.id, nextRunAt, null, 'blocked').catch((err) => {
    +   ctx.waitUntil(log(env, 5, 'schedule', {
    +     event: 'schedule.advance_failed',
    +     scheduleId: schedule.id,
    +     tenantId,
    +     url: schedule.url,
    +     blockReason: 'threat',
    +     errorClass: err?.constructor?.name,
    +     errorMessage: String(err?.message ?? '').slice(0, 128),
    +   }) ?? Promise.resolve());
    + });
    ```

    Conventions copied from sibling block (do not change them):
    - Severity 5 (error) -- matches `schedule.execute_fail` D1-write-failure shape.
    - Subsystem `'schedule'` (NOT `'scheduler'`) -- matches every other event in
      this file. Note ROADMAP.md success criterion #2 uses the informal name
      `scheduler:advance_failed`; the established subsystem in code is `'schedule'`.
      Match the codebase, not the informal prose.
    - `String(err?.message ?? '').slice(0, 128)` -- 128-char clamp is project-wide
      (CONVENTIONS.md §4).
    - `ctx.waitUntil(... ?? Promise.resolve())` envelope -- log() may return undefined.
    - `blockReason: 'threat'` distinguishes this from any future block reasons
      (quota, paused).

    2) `test/scheduled-handler.test.js` -- run the verify-then-add gate; if no
       existing case covers the threat-block + advanceSchedule-throw path, add
       one. Use the existing `makeController`, `runScheduled`, `cleanDb`,
       `seedSchedule`, `seedApiKey` helpers (lines 32-68 of that file). Stub
       `advanceSchedule` to throw (or wrap env.DB with a prepared statement
       that rejects on the schedule UPDATE) and capture log emissions via the
       project's existing log spy pattern (look in test for the convention --
       likely a vi.spyOn on the log module).

    Commit message (atomic):
    `fix(scheduler): log schedule.advance_failed instead of silent catch (PRE-02)`
    Body should reference: "fixes silent swallowing of D1 write failures on
    threat-blocked schedule advance (CONCERNS.md §Silent Catch). Demonstrates
    QG-06 logging discipline."

    NOTE on operational impact (per CONTEXT.md D-14 + RESEARCH.md Risk 1):
    after deploy, previously-silent D1 write failures on threat-blocked schedule
    advances will start showing up in Coralogix. This is the entire point of the
    fix; Plan A's deploy gate (Task A-04) includes a 24-hour observation window
    on staging before promoting to production.
  </action>
  <verify>
    <automated>
    # QG-06 grep gates + targeted unit test:
    bash -c '
      set -e
      # No silent catches anywhere in scheduler.js:
      ! grep -nE "\\.catch\\(\\(\\) ?=> ?\\{\\}\\)" src/scheduler.js || { echo "FAIL: silent catch still present"; exit 1; }
      # No console.* added:
      ! grep -nE "console\\.(log|warn|error|debug)" src/scheduler.js || { echo "FAIL: console.* added to scheduler.js"; exit 1; }
      # The new event + structured fields exist verbatim in the file:
      grep -q "event: '"'"'schedule.advance_failed'"'"'" src/scheduler.js || { echo "FAIL: schedule.advance_failed event missing"; exit 1; }
      grep -q "blockReason: '"'"'threat'"'"'" src/scheduler.js || { echo "FAIL: blockReason field missing"; exit 1; }
      grep -q "log(env, *5, *'"'"'schedule'"'"'" src/scheduler.js || { echo "FAIL: log(env, 5, schedule, ...) shape missing"; exit 1; }
      echo "OK: PRE-02 grep gates passed"
    '
    # Targeted unit test (per CLAUDE.local.md: targeted run only, NOT full npm test):
    npm test -- test/scheduled-handler.test.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "\.catch\(\(\) ?=> ?\{\}\)" src/scheduler.js` returns ZERO lines.
    - `grep -nE "console\.(log|warn|error|debug)" src/scheduler.js` returns ZERO lines (or only pre-existing exempted lines per CONCERNS.md table).
    - `grep -c "schedule.advance_failed" src/scheduler.js` returns 1 (one occurrence: the event field value).
    - `grep -c "blockReason: 'threat'" src/scheduler.js` returns 1.
    - The new emission uses severity 5 and subsystem `'schedule'` (matches sibling `schedule.execute_fail` at lines 161-172). Confirmed via `grep -B2 -A6 "schedule.advance_failed" src/scheduler.js | grep -E "(log\(env, *5,|subsystem|'schedule')"`.
    - `npm test -- test/scheduled-handler.test.js` passes (after verify-then-add of the new test case if one is needed; the case asserts the structured log call shape).
  </acceptance_criteria>
  <done>
    Atomic commit `fix(scheduler): log schedule.advance_failed instead of silent catch (PRE-02)`
    is created. The threat-block silent catch is gone; the new event matches the
    sibling pattern; targeted unit test passes. Ready for staging deploy in Task A-04.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task A-03: PRE-03 -- Rename formatDate in ui-submit.js</name>
  <files>src/ui/ui-submit.js</files>
  <read_first>
    - src/ui/ui-submit.js (lines 20-30 declaration + line 438 callsite)
    - src/ui/ui-settings.js (lines 18-30 -- confirm byte-identical body; this file is NOT modified)
    - .planning/phases/00-foundation/00-RESEARCH.md §PRE-03 (exact diffs)
    - .planning/phases/00-foundation/00-PATTERNS.md §"src/ui/ui-submit.js (modify, PRE-03)"
    - CLAUDE.md §"Dashboard UI Architecture" (UI prefix rule)
  </read_first>
  <action>
    Re-verify the bodies of `formatDate` in `ui-settings.js:22` and `ui-submit.js:25`
    are byte-identical (RESEARCH.md Risk 3 mitigation). If they differ unexpectedly,
    STOP and surface as a blocker.

    Then rename ONLY `ui-submit.js`'s copy (per D-CONTEXT "PRE-03 disambiguation":
    submit-side has 1 caller vs settings-side's 3, smaller blast radius).

    Exactly two line edits in `src/ui/ui-submit.js`:

    Line 25 (declaration):
    ```diff
    - function formatDate(isoStr) {
    + function submit_formatDate(isoStr) {
    ```

    Line 438 (callsite):
    ```diff
    - var resetDate = data.resetsAt ? formatDate(data.resetsAt) : '';
    + var resetDate = data.resetsAt ? submit_formatDate(data.resetsAt) : '';
    ```

    Do NOT touch `src/ui/ui-settings.js` -- its three callers (lines 291, 372, 723)
    keep using the unprefixed name, which is now globally unique because the
    submit-side twin has been prefixed.

    Commit message (atomic):
    `refactor(ui-submit): rename formatDate to submit_formatDate per UI prefix rule (PRE-03)`
    Body: "Eliminates duplicate-name violation of UI prefix rule (CLAUDE.md §Dashboard UI Architecture).
    Bodies were byte-identical, so behavior is unchanged. Demonstrates QG-07."
  </action>
  <verify>
    <automated>
    # QG-07 grep gates:
    bash -c '
      set -e
      # Only one formatDate declaration remains in src/ui/, in ui-settings.js:
      MATCHES=$(grep -nE "function +formatDate\\b" src/ui/*.js)
      echo "$MATCHES"
      [ "$(echo "$MATCHES" | grep -c -v "^$")" = "1" ] || { echo "FAIL: more than one formatDate declaration"; exit 1; }
      echo "$MATCHES" | grep -q "src/ui/ui-settings.js:" || { echo "FAIL: surviving formatDate is not in ui-settings.js"; exit 1; }
      # Submit-side now uses the prefixed name in both declaration and callsite:
      grep -nE "\\bsubmit_formatDate\\b" src/ui/ui-submit.js | grep -E ":(25|438):" || { echo "FAIL: missing submit_formatDate at expected lines"; exit 1; }
      # No bare formatDate left in ui-submit.js:
      ! grep -nE "\\bformatDate\\b" src/ui/ui-submit.js || { echo "FAIL: old formatDate name still present in ui-submit.js"; exit 1; }
      # Settings-side untouched:
      git diff --name-only HEAD~1 src/ui/ui-settings.js | grep -q ui-settings || true  # not strict; just informational
      echo "OK: PRE-03 rename complete"
    '
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "function +formatDate\b" src/ui/*.js` returns exactly one line, in `src/ui/ui-settings.js`.
    - `grep -nE "\bsubmit_formatDate\b" src/ui/ui-submit.js` returns 2 matches (line 25 decl + line 438 call).
    - `grep -nE "\bformatDate\b" src/ui/ui-submit.js` returns ZERO matches.
    - `git diff --stat src/ui/ui-submit.js` shows a +2/-2 stat (two lines edited).
    - `src/ui/ui-settings.js` is NOT modified by this commit.
  </acceptance_criteria>
  <done>
    Atomic commit `refactor(ui-submit): rename formatDate to submit_formatDate per UI prefix rule (PRE-03)`
    is created with exactly two line changes. All grep gates pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task A-04: Plan A deploy gate -- staging smoke + grace-period banner visual confirmation</name>
  <what-built>
    Three atomic commits land on a single PR (PRE-01, PRE-02, PRE-03). Merging the
    PR triggers staging deploy. The combined effect:
    - Billing dashboard for grace_period/blocked tenants now renders the correct
      `billing-status-banner alert`, not the stray "Status: Pending" detail banner.
    - Scheduler emits `schedule.advance_failed` (severity 5) instead of silently
      swallowing D1 write failures on threat-blocked advances.
    - `formatDate` collision in `src/ui/` is resolved.
  </what-built>
  <how-to-verify>
    Per CONTEXT.md "PRE deploy gate" + RESEARCH.md Risk 1 (24h observation window).

    1. Confirm CI is green on the PR; merge to main; staging auto-deploy completes.

    2. Run smoke test against staging:
       ```bash
       ./scripts/smoke-test.sh
       ```
       Expected: exits 0. If it fails, do NOT promote to production -- diagnose
       first.

    3. Visual confirmation of PRE-01 (grace-period banner). Two paths -- pick one:

       Path A (preferred -- real tenant in grace_period):
       - Open https://staging.webresourceledger.com/ui as an admin.
       - Navigate to a tenant that is currently in `billing_status='grace_period'`
         (or `'blocked'`).
       - Confirm: the billing view renders the alert banner with grace-period copy
         (NOT the stray "Status: Pending" detail-style banner that appeared before
         the fix).

       Path B (synthetic, only if no real tenant qualifies):
       ```bash
       source ~/.wrl-keys
       # Pick a low-volume staging tenant; synthetically flip billing_status:
       npx wrangler d1 execute wrl-staging --remote \
         --command "UPDATE tenants SET billing_status='grace_period' WHERE id='<test-tenant-id>'"
       # Open the dashboard for that tenant; observe banner.
       # IMMEDIATELY revert:
       npx wrangler d1 execute wrl-staging --remote \
         --command "UPDATE tenants SET billing_status='active' WHERE id='<test-tenant-id>'"
       ```

    4. PRE-02 sanity check on staging Coralogix (per CLAUDE.local.md, query via
       WRL_CORALOGIX_API_KEY against EU2):
       ```bash
       source ~/.secrets
       # DataPrime probe -- expect zero rows initially (no errors yet) but the
       # query must succeed (proves the field is reachable):
       cat > /tmp/probe.dataprime <<'EOF'
       source logs
       | filter $l.applicationname == 'wrl' && $l.subsystemname == 'schedule'
              && $d.event == 'schedule.advance_failed'
       | limit 5
       EOF
       curl -s -X POST "https://ng-api-http.eu2.coralogix.com/api/v1/dataprime/query" \
         -H "Authorization: Bearer $WRL_CORALOGIX_API_KEY" \
         -H "Content-Type: application/json" \
         -d "{\"query\":\"$(cat /tmp/probe.dataprime | tr '\n' ' ')\"}" | jq .
       ```
       Expected: API returns 200; result set may be empty (no failures yet) or
       contain real `schedule.advance_failed` events. EITHER outcome is acceptable.

    5. 24-hour observation window: leave Plan A on staging for 24h. Re-run the
       Coralogix query above. If `schedule.advance_failed` count is suspiciously
       high (>10/day on staging), investigate D1 health BEFORE promoting to
       production.

    6. After 24h with healthy signal, promote to production via the existing
       release path (the `deploy-staging.yml` workflow auto-handles staging; for
       production, use the `deploy-production.yml` workflow per repo convention).
       Re-run smoke against production URL: `./scripts/smoke-test.sh`
       (with PRODUCTION_URL env var set per script convention -- see script
       header).

    7. Confirm one more time on production: a tenant in grace_period sees the
       correct banner.
  </how-to-verify>
  <resume-signal>
    Reply "approved" to confirm:
    - smoke green on staging,
    - PRE-01 banner visually verified (real or synthetic tenant),
    - PRE-02 Coralogix probe succeeded with no anomalous error volume in 24h,
    - production deploy executed and smoke green on prod.

    OR describe the specific failure mode (smoke red, banner still wrong,
    Coralogix flood, etc.) and whether to roll back or investigate.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task A-05: QG-04+05 -- Write evolution log 0108-pre-flight-cleanup and update backlog</name>
  <files>
    docs/evolution/0108-pre-flight-cleanup/prompt.md,
    docs/evolution/0108-pre-flight-cleanup/decisions.md,
    docs/evolution/0108-pre-flight-cleanup/outcome.md,
    docs/evolution/README.md,
    docs/backlog.md
  </files>
  <read_first>
    - docs/evolution/0107-stripe-authoritative-billing/prompt.md, decisions.md, outcome.md (analog -- confirm shape)
    - docs/evolution/README.md (index format)
    - docs/backlog.md (tier conventions, item format -- top of file)
    - .planning/phases/00-foundation/00-PATTERNS.md §"docs/evolution/0108-pre-flight-cleanup/..." and §"docs/backlog.md"
    - CLAUDE.md §"Evolution Log" rules 1-7
    - .planning/phases/00-foundation/00-CONTEXT.md (for Plan A briefing content)
  </read_first>
  <action>
    Verify the evolution log number first:
    ```bash
    ls docs/evolution/ | grep -E '^0[0-9]{3}-' | sort | tail -3
    ```
    Expected: most recent is `0107-stripe-authoritative-billing`. Use `0108-pre-flight-cleanup`.
    If max is unexpectedly higher (autonomous-run gap), use max+1 instead and
    note in `outcome.md`.

    Create three files under `docs/evolution/0108-pre-flight-cleanup/`:

    `prompt.md`:
    - H1: `# Phase 0108: Pre-Flight Cleanup (Phase 0, Plan A)`
    - `## Task` -- the Plan A briefing in 1-2 paragraphs (PRE-01/02/03 surgical
      fixes; references CONCERNS.md §Scope Collision and §Silent Catch).
    - `## Approach` -- "Three atomic commits in one PR; staging smoke + visual
      banner gate; 24h observation; production promote."
    - `## Prior Work` -- bullet list referencing `.planning/phases/00-foundation/00-CONTEXT.md`
      (D-13/D-14) and `.planning/phases/00-foundation/00-RESEARCH.md` (PRE-01/02/03 sections).

    `decisions.md` (one H2 per discrete decision; **Decision** in bold + rationale):
    - "## Rename only the billing-side `buildStatusBanner` (not detail-side)" --
      Decision: Rename only ui-billing.js's copy. Rationale: detail-side is the
      survivor in flat scope, has 4 callers in detail.js (lines 34, 412, 552, 642),
      and renaming it would force changes across more callsites for no behavior
      gain. Per CONTEXT.md PRE-01.
    - "## PRE-03: rename submit-side, not settings-side" -- Decision: Rename
      `formatDate` in ui-submit.js (1 caller). Rationale: smaller blast radius
      than ui-settings.js (3 callers); bodies are byte-identical, so behavior
      is unchanged either way. Per CONTEXT.md "PRE-03 disambiguation."
    - "## Severity 5 + subsystem `'schedule'` for `schedule.advance_failed`" --
      Decision: Match sibling `schedule.execute_fail` at scheduler.js:161-172.
      Rationale: D1 write failures are severity 5 across the codebase; `'schedule'`
      is the established subsystem for every event in this file. ROADMAP.md
      success criterion #2 uses `scheduler:advance_failed` informally; we match
      the codebase, not the prose.
    - "## Stage-then-promote with 24h observation window" -- Decision: Hold on
      staging for 24h before promoting to production. Rationale: PRE-02 makes
      previously-silent D1 errors visible; if there's a latent D1 issue, we want
      it to surface on staging first (RESEARCH.md Risk 1).

    `outcome.md`:
    - H1: `# Outcome -- Phase 0108`
    - `## What was built` -- 3-5 bullets summarizing the three fixes and the
      QG-04..07 demonstration value.
    - `## Files changed` -- table:
      | File | Change |
      |------|--------|
      | `src/ui/ui-billing.js` | Renamed `buildStatusBanner` -> `billing_buildStatusBanner` (PRE-01) |
      | `src/scheduler.js` | Replaced silent catch on `advanceSchedule` with `schedule.advance_failed` log emit (PRE-02) |
      | `src/ui/ui-submit.js` | Renamed `formatDate` -> `submit_formatDate` (PRE-03) |
      | `test/scheduled-handler.test.js` | Added test case for advanceSchedule-throw -> log emit (if not pre-existing) |
    - `## QG metric snapshot` -- record:
      - QG-06: `grep -c '\.catch(() => {})' src/scheduler.js` = 0
      - QG-07: `grep -c 'function buildStatusBanner' src/ui/*.js` = 1 (in ui-detail.js); `grep -c 'function formatDate' src/ui/*.js` = 1 (in ui-settings.js)
      - Banner visual confirmation: passed on tenant `<id>` at `<timestamp>` (or "synthetic preview").
      - Coralogix `schedule.advance_failed` 24h count on staging: `<n>` events.
    - `## Backlog changes` -- per QG-05 + RESEARCH.md §QG-05. Most likely: "No
      backlog changes -- the three pre-flight fixes were not previously on the
      backlog (PRE-01 was an active bug, PRE-02 was a CONCERNS.md item, PRE-03
      was a convention violation). The absence is the record." If review of
      `docs/backlog.md` surfaces newly-relevant items, list them.

    Update `docs/evolution/README.md` -- append entry under whatever index
    section the file uses (read its current shape first; typical: a chronological
    bullet list or table). Add:
    ```markdown
    - [0108: Pre-Flight Cleanup](0108-pre-flight-cleanup/) -- PRE-01/02/03 surgical fixes (Phase 0, Plan A)
    ```

    `docs/backlog.md`: review the file. If any items are now resolved by Plan A
    (unlikely -- the PRE fixes weren't backlog items), strike through and add
    `-- DONE: <one-line outcome>`. If no changes, do not edit; the
    `outcome.md` already records "no backlog changes."

    Commit message:
    `docs(evolution): add 0108-pre-flight-cleanup phase log (QG-04, QG-05)`
  </action>
  <verify>
    <automated>
    bash -c '
      set -e
      # QG-04: all three required files exist:
      for f in prompt.md decisions.md outcome.md; do
        [ -f "docs/evolution/0108-pre-flight-cleanup/$f" ] || { echo "FAIL: missing $f"; exit 1; }
      done
      # outcome.md has both required H2 sections:
      grep -q "^## Files changed" docs/evolution/0108-pre-flight-cleanup/outcome.md || { echo "FAIL: outcome.md missing Files changed section"; exit 1; }
      grep -q "^## Backlog changes" docs/evolution/0108-pre-flight-cleanup/outcome.md || { echo "FAIL: outcome.md missing Backlog changes section (QG-05)"; exit 1; }
      # outcome.md references all three PRE IDs:
      for id in PRE-01 PRE-02 PRE-03; do
        grep -q "$id" docs/evolution/0108-pre-flight-cleanup/outcome.md || { echo "FAIL: outcome.md missing $id reference"; exit 1; }
      done
      # README.md updated:
      grep -q "0108-pre-flight-cleanup" docs/evolution/README.md || { echo "FAIL: README.md not updated"; exit 1; }
      echo "OK: QG-04 + QG-05 evolution log + index complete"
    '
    </automated>
  </verify>
  <acceptance_criteria>
    - `docs/evolution/0108-pre-flight-cleanup/{prompt,decisions,outcome}.md` all exist and are non-empty.
    - `outcome.md` contains both `## Files changed` and `## Backlog changes` sections.
    - `outcome.md` references PRE-01, PRE-02, PRE-03 by ID.
    - `outcome.md` records the QG metric snapshot (grep counts and banner visual confirmation result).
    - `docs/evolution/README.md` includes a new entry for `0108-pre-flight-cleanup`.
    - If `docs/backlog.md` was modified, the modification is intentional and matches the strikethrough-on-close convention; otherwise it is unchanged and `outcome.md` explicitly records "no backlog changes."
  </acceptance_criteria>
  <done>
    Evolution log entry `0108-pre-flight-cleanup` is committed with all three
    files, outcome.md has the QG-05 backlog section, and `docs/evolution/README.md`
    is updated. Plan A is fully closed: code shipped, deployed, observed, and
    documented per QG-04..07.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Worker (`api.webresourceledger.com`) | Tenant-authenticated dashboard requests. PRE-01 changes only an internal UI string; no new boundary or input. |
| Worker -> D1 (capture/schedule writes) | PRE-02 changes how D1 write FAILURE is handled. The boundary itself is unchanged; what's new is that the failure is now observable via Coralogix. |
| Worker -> Coralogix (logs) | New log line `schedule.advance_failed` flows through this existing channel. Same auth (`CORALOGIX_SEND_KEY`), same destination, same shape as `schedule.execute_fail`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00A-01 | Information Disclosure | `schedule.advance_failed` log payload (PRE-02) | mitigate | `errorMessage` is clamped to 128 chars via `String(err?.message ?? '').slice(0, 128)` -- matches project-wide convention (CONVENTIONS.md §4). `tenantId` and `url` were already in the sibling `schedule.blocked_threat` event at the same site (line 139-145), so no new sensitive field is exposed. `errorClass` is just a constructor name. |
| T-00A-02 | Denial of Service | Coralogix log volume (PRE-02 surfaces previously-silent errors) | mitigate | RESEARCH.md Risk 1: stage-then-promote with 24h observation window (Task A-04). If `schedule.advance_failed` floods, investigate D1 health before production deploy. The log severity is 5 (error) so it's already at the operational alert tier. |
| T-00A-03 | Repudiation | Audit trail for billing-state changes (PRE-01) | accept | PRE-01 is a UI-rendering fix only. No state mutation, no API surface change. The fix simply makes the existing billing-state alert visible. No new audit-trail concern. |
| T-00A-04 | Tampering | UI function shadowing (PRE-01, PRE-03) | mitigate | The flat-scope shadowing was the bug, not a security control. Renaming with the prefix convention (per CLAUDE.md §"Dashboard UI Architecture") closes the shadowing class entirely for these two functions. QG-07 grep gates enforce no future regression in this PR. |
| T-00A-05 | Spoofing | Authentication for new log emission (PRE-02) | accept | The new emission flows through the worker's existing `CORALOGIX_SEND_KEY`. No new credential, no new auth path. |
| T-00A-06 | Elevation of Privilege | None | accept | None of the three fixes introduces new authorization or privilege checks. PRE-01 is render-only; PRE-02 is observability-only; PRE-03 is a rename. |
</threat_model>

<verification>
## Plan A Phase Checks

After all 5 tasks complete:

1. **Code state** (post-merge to main, post-staging-deploy):
   - `grep -nE "function +buildStatusBanner\b" src/ui/*.js` returns one line in `ui-detail.js`.
   - `grep -nE "function +formatDate\b" src/ui/*.js` returns one line in `ui-settings.js`.
   - `grep -nE "\.catch\(\(\) ?=> ?\{\}\)" src/scheduler.js` returns ZERO.
   - `grep -nE "console\.(log|warn|error|debug)" src/scheduler.js` returns ZERO new occurrences.

2. **Test state** (post-merge):
   - `npm test -- test/scheduled-handler.test.js` passes (with new PRE-02 case if added).

3. **Deploy state** (per Task A-04):
   - Staging smoke green.
   - Banner visual confirmation logged in `outcome.md` with timestamp + tenant.
   - 24h observation: Coralogix `schedule.advance_failed` count noted in `outcome.md`.
   - Production smoke green.

4. **Documentation state** (per Task A-05):
   - `docs/evolution/0108-pre-flight-cleanup/{prompt,decisions,outcome}.md` exist.
   - `outcome.md` has `## Files changed` and `## Backlog changes` sections.
   - `docs/evolution/README.md` includes the new entry.

5. **PR state**:
   - One PR with three atomic commits (PRE-01, PRE-02, PRE-03), plus the
     evolution-log + backlog commit.
   - No `--no-verify` skips on hooks.
</verification>

<success_criteria>
1. The three PRE bugs (UI scope collision, scheduler silent catch, formatDate
   duplicate) are fixed and deployed to production via stage-then-promote with
   24h observation.
2. Banner visual confirmation passes for at least one grace_period or blocked
   tenant (real or synthetic) on both staging and production.
3. `schedule.advance_failed` log line is observable in Coralogix on production
   (zero or more occurrences depending on real D1 traffic; the field is reachable).
4. Evolution log entry `0108-pre-flight-cleanup` is committed with all three
   required files; `outcome.md` includes the QG metric snapshot and backlog
   review (per QG-04 + QG-05).
5. QG-06 and QG-07 grep gates are green at the end of this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-01-SUMMARY.md` with:
- What shipped (PRE-01, PRE-02, PRE-03 + evolution log).
- Final QG-06 and QG-07 grep counts.
- Banner visual confirmation result (tenant ID, timestamp, real vs synthetic).
- Coralogix 24h `schedule.advance_failed` count after staging deploy.
- Production smoke result.
- Reference to commit SHAs for the three atomic commits.
- Note that Plan B (00-02-PLAN) may now begin -- the post-PRE-02 baseline is the
  intended audit window per CONTEXT.md D-14.
</output>
