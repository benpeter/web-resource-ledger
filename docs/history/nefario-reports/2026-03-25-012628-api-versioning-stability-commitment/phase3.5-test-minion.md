ADVISE

- [testing]: The `node:fs` fallback in the version consistency test is correct to flag, but the fallback instruction itself needs clarification.
  SCOPE: `test/security-headers.test.js` -- Step 2, the openapi.yaml version consistency test
  CHANGE: The prompt says "if node:fs is not available, use package.json instead." The correct guidance should be: use `import pkg from '../package.json' with { type: 'json' }` as the *primary* approach, not a fallback. The `node:fs` / `readFileSync` pattern does not work in the Cloudflare Workers vitest pool (all unit tests run in the Workers runtime, not Node.js -- only integration `globalSetup` files run in Node.js). No existing unit test uses `node:fs`. The agent will waste effort trying `node:fs` first before discovering it fails. The prompt should lead with `import pkg from '../package.json'`.
  WHY: `node:fs` is unavailable in the Workers pool. The plan's note says "verify which imports work before committing" -- but this introduces unnecessary ambiguity and risks a mis-implementation that compiles but silently skips the assertion (e.g., `match` being null causing a misleading failure message rather than a clear import error). Directing the agent to try the right thing first avoids a debugging loop.
  TASK: Task 4

- [testing]: The ROUTE_KEYS coverage test (test case 4 in deprecation.test.js) will fail if ROUTE_KEYS is defined inside the fetch handler closure and not exported from src/index.js.
  SCOPE: `test/deprecation.test.js` -- test case "ROUTE_KEYS map covers all routes"
  CHANGE: Task 2 should either (a) export `ROUTE_KEYS` from `src/index.js` so it is importable in tests, or (b) export it from a separate module. If it remains a module-scope `const` that is not exported, the test cannot import it. Task 4's prompt should note this dependency explicitly and instruct the agent to import from the correct location -- or, if ROUTE_KEYS turns out to be unexported, to restructure the test to verify coverage indirectly (e.g., verify that SELF.fetch to each known route does NOT return a Deprecation header, which exercises the full lookup chain without needing direct access to the map). The plan's current test description assumes ROUTE_KEYS is importable but Task 2 does not explicitly require exporting it.
  WHY: Without an export, the test either cannot compile or must use fragile module introspection. The test is worth keeping -- a routes/ROUTE_KEYS drift is a real operational hazard (silent failure to inject deprecation headers on a deprecated route). The implementation and test tasks need to agree on whether ROUTE_KEYS is exported.
  TASK: Task 2 and Task 4

- [testing]: The deprecation unit test for "deprecated route produces correct headers" (test case 2) is testing logic that is embedded in the fetch handler, not an extracted function -- yet the prompt describes these as "pure function tests where possible."
  SCOPE: `test/deprecation.test.js` -- test cases 2 and 3 (header format and Link concatenation)
  CHANGE: The prompt already acknowledges this risk ("if the deprecation logic is embedded in the fetch handler, you may need to test via SELF.fetch with a test-only deprecated route") and then rules out the SELF.fetch approach because DEPRECATIONS is a static import that cannot be overridden per-test. This creates a gap: the format correctness of the Deprecation (`@timestamp`) and Sunset (HTTP-date) headers is untested unless the logic is extracted to a pure function. Task 4 should explicitly extract a `buildDeprecationHeaders(dep)` helper (or similar) from the post-response block -- even a 5-line function -- so the format tests can be pure. Without this, test cases 2 and 3 reduce to dead code or get silently omitted by the agent.
  WHY: The header format is load-bearing -- the RFC 9745 `@timestamp` Structured Field Date format is non-obvious and easy to get wrong (e.g., emitting a plain integer or an HTTP-date instead). If format correctness is not tested at v1.0.0, the first real deprecation event will be the first time the format is validated in production.
  TASK: Task 4 (with a note to Task 2 to extract the helper)
