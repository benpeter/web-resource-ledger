---
task: "R2: Key versioning and public key archive"
source-issue: 32
date: 2026-03-16
slug: key-versioning
mode: execution
task-count: 2
gate-count: 0
specialists: security-minion, test-minion
reviewers: lucy, margo, security-minion, test-minion, ux-strategy-minion
---

## Summary

Added key versioning to the Ed25519 WACZ signing flow, enabling signing key
rotation without breaking verification of previously-signed captures. Each WACZ
signature now includes a `keyId` (SHA-256 fingerprint, 8 hex chars), historical
signing keys are archived in KV, the verification endpoint resolves the correct
historical key from the capture's KV record, and a new `/.well-known/signing-keys`
endpoint serves the public key archive. 13 new tests cover key rotation, fallback
paths, and archive operations. All 409 tests pass.

## Original Prompt

Outcome: The signing key can be rotated without breaking verification of prior
captures, removing a critical data integrity risk and the bold README warning
about key rotation.

Success criteria:
- Every WACZ signature includes a keyId field (SHA-256 fingerprint, truncated to 8 hex chars)
- /.well-known/signing-keys endpoint serves historical public keys
- Verification endpoint reads keyId from WACZ and selects the correct historical key
- Key rotation procedure: generate new key, deploy, old key automatically archived in KV
- All existing captures remain verifiable after a key rotation
- Tests cover: signing with new key, verifying with old key, key archive retrieval

## Key Design Decisions

1. **keyId = 8 hex chars of SHA-256(raw public key)** — deterministic fingerprint,
   sufficient for single-digit key counts. Security comes from Ed25519 signature,
   not the fingerprint.

2. **verifyWacz() unchanged** — pure function keeps `(waczBytes, publicKeyBytes)`
   signature. Key resolution in handler preserves the trust model where publicKeyBytes
   always comes from the server.

3. **keyId from KV record, not WACZ** — server-side verification uses the KV capture
   record's keyId (server-controlled). WACZ-embedded keyId is for offline verifiers.

4. **Archive before completeCapture()** — eliminates race window where KV record
   references a keyId whose archived key doesn't exist yet.

5. **Legacy fallback = current key only** — captures without keyId fall back to
   current signing key. No brute-force iteration over all archived keys.

## Phases

### Phase 1: Meta-Plan
Identified security-minion and test-minion as specialists. Small team appropriate
for a focused cryptographic feature with clear scope boundaries.

### Phase 2: Specialist Planning
- **security-minion**: Identified the critical constraint that key resolution must
  stay in the handler, not move into verifyWacz(). Recommended 32-byte validation
  on archive writes and flagged "try all keys" fallback as a trust model weakness.
- **test-minion**: Recommended multi-key test setup via vitest.config.js, identified
  5 test gaps including fallback failure paths and ordering assertions.

### Phase 3: Synthesis
Resolved tensions between issue spec and security review. Key resolution from KV
record (security-minion) over WACZ content (issue wording). Kept 8 hex chars (issue
spec) over 16 (security-minion) since keyId is a lookup index. Removed "try all
archived keys" per security-minion and margo.

### Phase 3.5: Architecture Review
5 reviewers: 1 APPROVE (ux-strategy-minion), 4 ADVISE (lucy, margo, security-minion,
test-minion), 0 BLOCK. All ADVISEs addressed in implementation. No revision rounds
needed.

### Phase 4: Execution
Two-task execution: core implementation then tests. First test run had 3 failures
(invalid capture ID format in test fixtures). Fixed and all 409 tests pass.

### Phases 5-8
Skipped per user directive (skip all post-exec).

## Agent Contributions

### Planning Agents
| Agent | Recommendation | Tasks |
|-------|---------------|-------|
| security-minion | Key resolution must stay server-side; validate key length; no brute-force fallback | 5 proposed |
| test-minion | Multi-key test setup; 5 test gap identifications; deterministic keyId requirement | 6 proposed |

### Review Agents
| Agent | Verdict | Key Finding |
|-------|---------|-------------|
| ux-strategy-minion | APPROVE | API naming coherent, backward-compatible |
| security-minion | ADVISE | Ensure handler never reads keyId from WACZ; validate 32-byte key |
| test-minion | ADVISE | Fallback failure path untested; archive ordering assertion needed |
| lucy | ADVISE | keyId length (not adopted); remove "try all" (already done); evolution log |
| margo | ADVISE | List endpoint may be YAGNI (kept per issue); remove "try all" (already done) |

## Execution

### Task 1: Core Implementation
Files modified: src/signing.js, src/kv.js, src/wacz.js, src/capture.js, src/index.js

### Task 2: Tests
Files modified: vitest.config.js, test/key-rotation.test.js (new), test/signing-key.test.js,
test/wacz.test.js

### Documentation
Files modified: openapi.yaml, docs/backlog.md, docs/evolution/README.md,
docs/evolution/0017-key-versioning/ (prompt, decisions, outcome, process)

## Verification

All 409 tests pass across 20 test files. OpenAPI spec validates cleanly.
New test coverage: keyId computation (3 tests), archive operations (3 tests),
rotation verification (3 tests), legacy fallback (1 test), failure paths (2 tests),
endpoint tests (1 test).

## Test Plan

- [x] computeKeyId returns 8 hex chars, deterministic, unique per key
- [x] archiveSigningKey stores and retrieves correctly
- [x] Idempotent archive (same keyId twice)
- [x] 32-byte validation rejects malformed keys
- [x] buildWacz includes keyId in signedData and return value
- [x] KV record includes wacz.keyId after capture
- [x] Signing key archived in KV after capture
- [x] Verification with keyId in KV → archived key succeeds
- [x] Legacy capture (no keyId) → current key fallback succeeds
- [x] Rotation: old WACZ + archived old key → verification succeeds
- [x] Deleted archived key + different current key → verified: false
- [x] /.well-known/signing-keys returns archived keys
- [x] /.well-known/signing-key includes keyId in response

## Session Resources

<details>
<summary>Skills Invoked</summary>

- `/nefario` — orchestration

</details>

<details>
<summary>Working Files</summary>

Companion directory: `docs/history/nefario-reports/2026-03-16-113015-key-versioning/`

Files:
- prompt.md — original task description
- phase1-metaplan.md — specialist selection and planning questions
- phase2-security-minion.md — security analysis and recommendations
- phase2-test-minion.md — test strategy and gap analysis
- phase3-synthesis.md — execution plan
- phase3.5-lucy.md — governance review (ADVISE)
- phase3.5-margo.md — complexity review (ADVISE)
- phase3.5-security-minion.md — security review (ADVISE)
- phase3.5-test-minion.md — test review (ADVISE)
- phase3.5-ux-strategy-minion.md — UX strategy review (APPROVE)

</details>

Context compaction events: 0
