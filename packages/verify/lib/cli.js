// Placeholder -- CLI orchestration (Task 3)
// Will parse arguments, load the WACZ file, resolve keys, run verifyWacz,
// and format output.

/**
 * Entry point for the wrl-verify CLI.
 *
 * @param {string[]} argv  process.argv.slice(2)
 */
export function run(argv) {
  process.stderr.write('wrl-verify: not yet implemented\n');
  process.exit(2);
}
